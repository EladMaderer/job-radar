# Pin the public npm registry for this project. Without this, running npm on a machine whose
# global npm config points at a private registry (e.g. a corporate Artifactory) rewrites
# package-lock.json with internal URLs that GitHub Actions runners can't fetch.
registry=https://registry.npmjs.org/
