{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run migrate && cd web && npm ci --no-audit --no-fund && npm run build",
  "outputDirectory": "web/dist",
  "framework": null,
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/index.html" }],
  "functions": {
    "api/resume/capture.ts": { "maxDuration": 300 },
    "api/jobs/[id]/tailor.ts": { "maxDuration": 300 },
    "api/jobs/[id]/prep.ts": { "maxDuration": 300 },
    "api/jobs/[id]/tailor/pdf.ts": { "maxDuration": 300 }
  }
}
